Generate bindings for `wlr/backend.h` as well as `wayland-server-core.h`.

Execute with `nix run` (or `cabal run` if you are in the development shell).

```console
$ ./generate-bindings
$ nix run
Detected output with description: X11 output 1
```
